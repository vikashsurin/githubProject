## ssh is more secure and easy.

# How to clone a git repo?
- via https, require email and password to push. 
- via ssh, does not require email and password to push.
- via manually downloading the code.
